
project Interfacing is

   for Object_Dir use "build";
   for Source_Dirs use (".");
   for Main use ("main.adb");
   for Languages use ("Ada", "C");

   package Compiler is
      for Default_Switches ("ada") use ("-g", "-gnatwa", "-gnata", "-fstack-check");
   end Compiler;

end Interfacing;

